<table>
  <tbody>
    <tr>
      <td class="text-center" style="width: 30%;">お名前:</td>
      <td><%= @contact.name %></td>
    </tr>
    <tr>
      <td class="text-center">ご返信用メールアドレス:</td>
      <td><%= @contact.email %></td>
    </tr>
    <tr>
      <td class="text-center">件名:</td>
      <td><%= @contact.subject %></td>
    </tr>
    <tr>
      <td class="text-center">お問合せ内容</td>
      <td><%= @contact.message %></td>
    </tr>
  </tbody>
</table>
<div>
  <p>こちらの内容でお送りしてよろしいですか？</p>
</div>

<%= form_with model: @contact, url: contacts_path, method: :post do |f| %>
  <%= f.hidden_field :name %>
  <%= f.hidden_field :email %>
  <%= f.hidden_field :subject %>
  <%= f.hidden_field :message %>
  <div><%= f.submit '送信' %></div>
<% end %>
<%= form_with model: @contact, url: back_contacts_path do |f| %>
  <%= f.hidden_field :name %>
  <%= f.hidden_field :email %>
  <%= f.hidden_field :subject %>
  <%= f.hidden_field :message %>
  <div><%= f.submit '入力画面に戻る' %></div>
<% end %>