<% if user != current_user %>
  <% if current_user.following?(user) %>
  <%= link_to user_relationships_path(user), method: :delete, local: false do %>
    <button class="btn btn-danger">フォロー解除</button>
  <% end %>
  <% else %>
  <%= link_to user_relationships_path(user), method: :post, local: false do %>
    <button class="btn btn-primary">フォロー</button>
  <% end %>
  <% end %>
<% end %>

