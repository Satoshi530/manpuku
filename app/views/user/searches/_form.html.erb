<div class="search_form">
  <%= form_with url: searches_path, local: true, method: :get do |f| %>
    <%= f.text_field :word %>
    <%= f.select :range, options_for_select([['User'], ['Post'], ['Tag']]) %>
    <%= f.submit "検索", class: "btn btn-secondary" %>
  <% end %>
</div>
