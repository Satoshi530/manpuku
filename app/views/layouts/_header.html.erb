<nav class="navbar navbar-expand-lg navbar-light text-dark bg-dark">
  <div class="logo">
    <%= link_to 'まんぷくっ', posts_path %>
  </div>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item active">
          <%= link_to "マイページ", user_path(current_user), class: 'btn btn-sm btn-outline-secondary nav-link text-light' %>
        </li>
        <li class="nav-item active">
          <%=link_to"ブックマーク", favorites_user_path(current_user), class: 'btn btn-sm btn-outline-secondary nav-link text-light' %>
        </li>
        <li class="nav-item active">
          <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: 'btn btn-sm btn-outline-secondary nav-link text-light' %>
        </li>
      <% else %>
        <li class="nav-item active">
          <%= link_to "まんぷくっとは？", about_path, class: 'btn btn-sm btn-outline-secondary nav-link text-light' %>
        </li>
        <li class="nav-item active">
          <%= link_to "新規登録", new_user_registration_path, class: 'btn btn-sm btn-outline-secondary nav-link text-light' %>
        </li>
        <li class="nav-item active">
          <%= link_to "ログイン", new_user_session_path, class: 'btn btn-sm btn-outline-secondary nav-link text-light' %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>